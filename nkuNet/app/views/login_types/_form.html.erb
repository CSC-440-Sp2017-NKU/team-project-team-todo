<%= form_for(login_type) do |f| %>
  <% if login_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(login_type.errors.count, "error") %> prohibited this login_type from being saved:</h2>

      <ul>
      <% login_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
